//https://www.codewars.com/kata/55d5da66a0e378b8bc0000c6


function getCharacters(obj, key, val) {
  const characters = obj.characters;
  return characters.filter(object => object[key] && object[key].toLowerCase() === val.toLowerCase());
}
